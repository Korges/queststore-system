{% extends "classpath:/templates/admin/nav.twig" %}

{% block content %}
    <div class="table-container">
        <table class="table">
            <thead>
            <tr>
                <th>#</th>
                <th>ID</th>
                <th>Level</th>
                <th>Exp</th>
                <th>Edit</th>
            </tr>
            </thead>
            <tbody>
            {% set ID = 1 %}
            {% for level in levels %}
            <tr>
                <td>{{ID}}</td>
                <td>{{level.id}}</td>
                <td>{{level.level}}</td>
                <td>{{level.experience}}</td>
                <td>
                    <form action = "" method = "post">
                        <button type="submit" name="id" value="{{ level.id }}" class="btn btn-default btn-sm">
                        <span class="glyphicon glyphicon-cog">edit</span>
                        </button>
                    </form>
                <td>
            <tr>
                {% set ID = ID + 1 %}
                {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}